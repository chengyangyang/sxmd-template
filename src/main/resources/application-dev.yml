### mysql 设置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sxmd_springboot?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Hongkong
    username: root
    password: admin
    type: com.zaxxer.hikari.HikariDataSource
    # 数据池
    hikari:
      pool-name: hikari-pool

